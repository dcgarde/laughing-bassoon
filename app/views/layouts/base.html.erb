<%# The "base" layout contains boilerplate common to *all* views. %>

<!DOCTYPE html>
<html>
  <head>
    <!-- Laughing Bassoon <%= Rails.application.config.version %> (<%= l(Rails.application.config.version_time) %>) -->
    <title>
      <%= strip_tags(yield(:title)) + " – " if content_for?(:title) %>
      Laughing Bassoon
    </title>
    <%# Specifies the default name of home screen bookmark in iOS %>
    <meta name="apple-mobile-web-app-title" content="Laughing Bassoon">

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@latest/css/pico.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield(:head) %>  </head>

  <body>
    <%= yield %>
  </body>
</html>
